import { css } from 'styled-components'

export const typography = css`
  /* font Sizes */
  :root {
    --h1: 36px;
    --h2: 24px;
    --h3: 20px;
    --s-medium: 1rem;
    --s-normal: 0.875rem;
    --s-small: 0.75rem;
  }
  /* font weights */
  :root {
    --bold: 700;
    --semi: 600;
    --medium: 500;
    --regular: 400;
    --light: 300;
  }
`
